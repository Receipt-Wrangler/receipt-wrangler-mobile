import { FeatureConfig } from '../api';
export declare class SetFeatureConfig {
    config: FeatureConfig;
    static readonly type = "[FeatureConfig] Set Feature Config";
    constructor(config: FeatureConfig);
}
