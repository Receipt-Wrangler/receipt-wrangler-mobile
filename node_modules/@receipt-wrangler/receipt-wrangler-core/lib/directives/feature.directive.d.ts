import { TemplateRef, ViewContainerRef } from '@angular/core';
import { Store } from '@ngxs/store';
import * as i0 from "@angular/core";
/**
 * Add the template content to the DOM unless the condition is true.
 */
export declare class FeatureDirective {
    private templateRef;
    private viewContainer;
    private store;
    private hasView;
    constructor(templateRef: TemplateRef<any>, viewContainer: ViewContainerRef, store: Store);
    set appFeature(feature: string);
    static ɵfac: i0.ɵɵFactoryDeclaration<FeatureDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<FeatureDirective, "[appFeature]", never, { "appFeature": { "alias": "appFeature"; "required": false; }; }, {}, never, never, false, never>;
}
